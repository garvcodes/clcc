# Git LFS tracking for large files

# Processed AnnData files (can be 100MB-1GB each)
*.h5ad filter=lfs diff=lfs merge=lfs -text
data/processed/*.h5ad filter=lfs diff=lfs merge=lfs -text

# Any large data files that might be added
*.h5 filter=lfs diff=lfs merge=lfs -text
*.hdf5 filter=lfs diff=lfs merge=lfs -text
*.parquet filter=lfs diff=lfs merge=lfs -text
